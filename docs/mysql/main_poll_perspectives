CREATE TABLE IF NOT EXISTS `main_poll_perspectives` (
	`id` int(10) NOT NULL auto_increment,
	`created` int(11),
	`user_id` int(10),
	`upvotes` int(10),
	`content` varchar(550),
	PRIMARY KEY( `id` )
);
