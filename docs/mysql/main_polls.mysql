CREATE TABLE IF NOT EXISTS `main_polls` (
	`id` int(10) NOT NULL auto_increment,
	`name` varchar(255),
	`description` varchar(550),
	`upvotes` int(10),
	`downvotes` int(10),
	`created` int(11),
	`author` int(7),
	PRIMARY KEY( `id` )
);
